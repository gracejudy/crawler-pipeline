# Changelog

모든 주요 변경사항은 이 파일에 기록합니다.

## [Unreleased]
- 프로젝트 루트 표준 파일 추가 (`.env.example`, `README.md`, `CHANGELOG.md`)
- 문서 중심 운영 구조 정리 (`docs/`)
- `backend → src` 마이그레이션 Step 1/2 적용 (`src/` 호환 래퍼 추가, 기존 경로 유지)
- 실행 스크립트 표준화 (`backend/package.json`에 `start`, `start:src` 추가)
